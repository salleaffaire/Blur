// Test some comments
using "IO"

struct a {
   var list{bit[32]} mList;

   func bit[32] foo(bit[32] x) {
      return x + 1;
   }

   struct b {
      func bit[16] foo(bit[32] x) {
         return x - 1;
      }
   }  
}

func void bar() {

}

func bit[32] main() {
   var a a_var1;
   var bit[32] a_var2;

   avar_2 = a_var1.->foo(->bar());

   ->bar();

   return a_var2;
}
