var list{bit[32]}[32] name1;
var bit[32]           name2;
var bit[32]           name3 if ( );

func bit[32] foo(bit[32] n1, list{bit[32]} l1) {
   if () {
   } 
   else { 
      if () {} 
      break;
   } 
}
