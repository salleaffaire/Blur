var list{bit[32]}[32] name1;
var bit[32]           name2;
var bit[32]           name3 if ( 0 );

func void bar() {
   return;
}

func bit[32] foo(bit[32] n1, list{bit[32]} l1) {
   name1 = 128;
   if (((0 == 0))) {
      while (name1 != 0) {
         var void name;
         name1 = -(1 * 5 / 2);
      }
      return;
   } 
   else { 
      if (name2 <= "name") {
         // Do something usefull
         var bit[32][128] foo_var if (name2 == 64);
         {
            foo_var = 0;
         }
      } 
      break;
   } 
}
